## Task

The board of Team Neptune decided to take over control of one e-commerce webserver from Team Saturn. The system is called `my-happy-shop`.

1. Search for the correct Pod in Namespace `saturn` (check annotations/labels for `my-happy-shop`)
2. Move it to Namespace `neptune`

It's okay to shut it down and spin it up again in the new namespace.
